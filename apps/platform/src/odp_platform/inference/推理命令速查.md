```bash
# 单张图片检测
odp-infer detect --model <模型路径> --input <图片路径>

# 批量检测（整个目录）
odp-infer detect --model <模型路径> --input <图片目录> --output <输出目录>

# 基准测试
odp-infer benchmark --model <模型路径> --image <测试图片> --iterations 50# 激活环境（不同机器按实际情况）
```